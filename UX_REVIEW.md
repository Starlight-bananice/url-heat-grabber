# 使用体验审查

改造基线：原版 0.5.2（`7e756b9`）。预览版使用独立的应用标识和本机数据目录。

## 本轮已实现

| 原有问题 | 新版行为 | 使用者获得的改善 |
| --- | --- | --- |
| 文件路径、参数和日志同时挤在一屏 | 工作台 / 任务记录 / 指南三级导航；输入与结果分区 | 新用户按添加、抓取、查看的顺序完成任务 |
| 进度条一直滚动，只能读日志猜进度 | 每条结构化结果更新真实数量、比例与用时 | 清楚已处理、待关注和未处理的数量 |
| 每次要进入文件夹找结果 | 窗口内结果表、搜索、筛选、直接打开 Excel | 先定位异常，再导出或交付 |
| 不支持的网站留空状态，容易误认为成功 | 不支持的网址明确标注，未判断有效性 | 避免将跳过项计为抓取成功 |
| 空白互动数与零难以区分 | 未获取显示“—”，零保留为 0，空数据单列状态 | 不把未知数据当作零或删除 |
| 导入 Excel 只能读第一列，表头也进入抓取 | 识别当前工作表所有列网址、超链接与 HYPERLINK 网址；表头不进入任务 | 减少手工清表步骤 |
| 分享文案、重复链接增加整理成本 | 从文案抽取网址，实时显示去重和无效内容数量 | 可直接粘贴；按需保留重复行 |
| CSV 原来按纯文本逐行读取 | 按 CSV 单元格解析，支持 UTF-8 BOM 和 GB18030 | 带标题、引号、逗号的表格更易导入 |
| 单一断点文件会被下一批覆盖 | 每次任务独立保存进度，按同批链接和模式恢复 | 多批工作能分别继续处理 |
| 中途异常也可能提示已保存 | 只有成功写出 Excel 后才显示保存完成；失败可另存 | 避免丢失结果却以为完成 |
| 停止后结果缺少尚未处理的链接 | 导出完整输入顺序，未处理行明确标注 | 交付表格可核对总量 |
| 抓取时可改输入和目录 | 运行中锁定任务参数，日志和结果仍可查看 | 避免实际任务与界面内容不一致 |
| 只能重跑整批 | 整理待处理项后手动开始；不自动高频重试 | 更少重复请求和等待 |
| 小窗口结果表过窄、可见行少 | 精简默认参数，提供“展开结果” | 适合长列表集中检查 |
| 设置每次重新选择 | 保存目录、模式、速度、去重偏好在本机记忆 | 减少重复操作 |
| 表格缺少阅读辅助 | 表头冻结、列筛选、链接可点击、状态说明、交替行色 | 导出文件可直接核对 |

## 建议下一轮优先做

1. **平台可用性预检和引导（优先级高）**：开始前检测 Chrome、网络及选中平台的已知限制；对小红书短链、B站短链提供可靠的展开流程。不能仅凭一个正常首页宣称该平台所有内容可抓取。
2. **首次浏览器准备状态（优先级高）**：当前显示正在准备组件，仍需补充下载阶段、可解释的错误及取消处理。Selenium 的初始化期间无法立即终止，停止需等待当前操作返回。
3. **真实样本回归与每个平台的能力说明（优先级高）**：用用户授权的最新链接分别验证有数据、无数据、删除、验证、受限等状态，明确每个平台能读到哪些指标。
4. **更大批次的性能（优先级中）**：把大型 Excel 导入与手动导出移到后台；对数万条结果使用虚拟列表，避免文件读取或全表更新占用界面线程。
5. **文件拖放与更方便的任务管理（优先级中）**：支持拖入文件、选择工作表、任务重命名与保留周期；删除历史需可恢复，避免误清理用户记录。
6. **跨机器交付（优先级中）**：Apple Developer ID 签名、公证、干净 Mac 首次启动测试，以及 Windows 的界面与打包回归。当前交付为本地 Mac 预览版本。

## 验证边界

本次重点验证界面、导入、结果语义、进度事件、停止、续跑和文件导出，并补充了单条真实贴吧帖子的四项指标及 Excel 导出验证。受控测试的示例链接及指标只用于验证软件流程，不能作为实际平台抓取成功率的证据。尚未进行全平台大批量回归或 Windows 实机验证。
